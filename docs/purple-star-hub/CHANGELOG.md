# Purple Star Astrology — build ledger

## Wave 5.3 addendum — 2026-07-07 — subnav rebuilt as a mega-menu

The PSA subnav dropdowns were flat vertical lists; rebuilt as multi-column mega-panels (research synthesized to `docs/purple-star-hub/research/nav/`). **Palaces** now opens a three-column grid of the twelve rooms, each a glyph seal (ruby characters) plus its English name, with a featured header and a "Browse all twelve" link; the cells keep their `data-palace` hooks so they still select the room in the court model. **Stars** opens two labeled family columns (Zi Wei group of six, Tian Fu group of eight) with per-star seals. **Study** and **Reference** open two-column lists with a glyph seal, name, and a one-line description each. Desktop gains hover-intent open (140ms in, 260ms out), guarded to fine-pointer devices so touch and keyboard are unaffected and click still toggles; Escape and focus-out still close. Right-side panels (Study, Reference) open leftward so a wide panel never runs off the viewport edge. Mobile (≤719px): the primary row stays a fade-masked horizontal scroller and every mega-panel collapses to a single full-width, scrollable column with 44px rows and no 320px overflow. Colorblind-safe throughout (glyph + text, never color alone); the top-level item count is unchanged (nav-budget-guard). Verified: hub inline JS parses (7 blocks), ruby/rt balanced (143/143), no duplicate ids, JSON-LD valid.

## Wave 5.2 addendum — 2026-07-07 — sitemap, robots, Course + FAQ schema

Closed the SEO/GEO items from the audit. **Sitemap:** added `glossary/` and `supporting-stars/` (the two Wave 3/4 pages that were missing), so all 22 PSA URLs are now listed; XML validates. **robots.txt:** the `Sitemap:` directive is now the absolute `https://www.zodianimal.com/sitemap.xml`. **Course schema:** the hub now carries a `Course` graph (name, provider, `isAccessibleForFree`, `teaches`, `hasCourseInstance` online, and eight `Syllabus` sections for the eight levels), since the hub is literally a structured course. **FAQPage schema:** the hub gained a visible six-question "Common questions" section (reusing the disclosure styling) with matching `FAQPage` JSON-LD, and honest `FAQPage` JSON-LD was added to four-forces, palaces, the stars index, and all 14 star pages, with answers drawn from each page's real content and generated per-star from the data (essence, transformation participation, the "no star is simply good or bad" framing). Nineteen PSA pages now carry FAQPage; all JSON-LD across all 22 pages parses; the hub has no duplicate ids, balanced script tags, and all inline JS still parses. Note: on the child/star pages the FAQ answers reflect on-page prose but are not rendered as a visible accordion (only the hub has the visible block), so they serve AI-answer engines more than Google rich results; adding visible FAQ blocks to those pages is optional future polish.

## Wave 5.1 addendum — 2026-07-07 — Mandarin SEO/GEO meta

Meta layer tuned for Mandarin discoverability (Baidu, Chinese-language and AI queries) across all 22 PSA pages, idempotent: **紫微斗數 now appears in every page title** (near the romanized "Zi Wei Dou Shu"), and in the meta description and og:title/og:description where present. Added `<meta name="keywords">` with a bilingual set (traditional 紫微斗數 plus the simplified alias 紫微斗数 for mainland reach, pinyin, English, ZWDS, 命盤), tailored per page (four-forces gets 四化/化祿/化權/化科/化忌; palaces get 十二宮/命宮; supporting-stars get 輔星/六吉星/六煞星; each star page gets its own traditional + simplified name + pinyin + editorial title). Added `og:locale` en_US with `og:locale:alternate` zh_TW, zh_CN, zh_HK. Fixed the lowercase-first meta descriptions on qi-sha, wu-qu, zi-wei, and added 紫微 to the zi-wei star title. No hreflang (there is no separate zh-language page; hreflang to a nonexistent URL would be wrong). All 22 pages still validate: JSON-LD parses, one head title each. **Honest ceiling:** the pages are English with Chinese terms, so this gets them found for the term names; ranking for full Mandarin sentence queries would need a genuine zh-Hant/zh-CN language version (Chinese H1 and lead copy), which is not built.

## Wave 5 — 2026-07-07 — SHIPPED

The Reader's Path and the sweep. Level 8 (teach-backs, ethics, consultation, the 20-question Imperial exam), a web-researched teach-back corpus, the master build doc, the full simplified-character and trust sweep of the child pages, the production audit, and the back-test and stress-test. Built per PSA-CURRICULUM Level 8 and PSA-MASTER-PLAN §5. **Uncommitted; commit manually.**

**Web-research teach-back corpus (NEW).** Five parallel research agents gathered how each core concept is taught on the open web and synthesized it (in the site's non-deterministic voice, no verbatim passages, sources listed) into `docs/purple-star-hub/research/teachbacks/`: `triangle.md`, `transformations.md`, `brightness.md`, `timing.md`, `ethics-and-reading-order.md`. This is the research layer the FABLE plan called for and did not contain; it feeds Level 8. Building on the FABLE 5.0 flow, not replacing it.

**Level 8 data (`ziwei-teachbacks.js`, NEW).** Five teach-back prompts, each with a beginner and a practitioner model answer and a four-point self-scoring rubric (accurate, non-deterministic, plain English, real vocabulary with its ladder). The reader's phrasebook (six "may say", five "may never say", plus the agency through-line 不問不答). The consultation structure as five steps, explicitly labeled house method, not classical authority. The 20-question Imperial exam covering the whole path (palaces, stars, transformations, triangle, brightness, timing, auxiliaries, ethics), authored fresh and grounded in docs 02-05 plus the research; every answer teaches structure.

**Hub Level 8 capstone (#readers-path).** The interactive teach-back model (student picks a prompt, writes, reveals the two-register model answer, self-scores against the rubric, submits a comprehension event); the phrasebook and consultation structure rendered as reference; the 20-question Imperial exam with per-question feedback and a rank scroll. Completing all five teach-backs and scoring 18+ marks Level 8 complete, which the store resolves to Imperial Astrologer. L8 un-gated in `ziwei-lessons.js`; its ladder link points to the capstone.

**The sweep.** 695 simplified→traditional replacements across 19 child pages, scoped to specific multi-character ZWDS terms so the sitewide simplified zodiac-animal glyphs were left untouched (verified). The four-forces season framing relabeled as a teaching metaphor per D6 (the element table and the seasons are named as two separate teachings that do not line up). Seven trust corrections applied via subagent against PSA-TERMINOLOGY §3: the four-forces Ren/Ding/Bing Hook determinism, the qi-sha Spouse line, the tian-liang shelter line (plus its meta and og descriptions), the history Chen Tuan attribution and superlative, and the zi-wei star page C1 transformation correction (Zi Wei never receives the Flow or the Hook; takes the Power in 壬 years and the Shine in 乙 years). A transparent note added to the chart page that Mei's Fire Bureau is a teaching choice where a strict 丙子 cast derives Metal.

**Master build doc (`PSA-MASTER-BUILD.md`, NEW).** The single record of the finished build: how it sits on the FABLE 5.0 flow, the data architecture, the pages, the casting engine, the seven-model interactive layer, the student-and-teacher teach-back loop, the progress and honesty rules, the back-test, the stress-test, the known deltas, and the FABLE acceptance checklist run against the result.

**Verified this session.** `ziwei-teachbacks.js` node-checks clean; all thirteen `ziwei-*.js` load together (39 namespaces, 20 exam questions, L8 un-gated). Hub inline scripts (7 blocks) parse; tags balanced (22/22); zero duplicate ids; ruby/rt balanced (133/133). **Caster stress-test: 4,320 casts, 0 invariant failures** (12 palaces distinct, 14 stars placed once each, Tian Fu mirror, valid bureau, 12 doors). **Independent production audit: 10 checks, 0 real issues** across 22 pages (the three banned-string hits are a myth-rejecting quiz distractor, the overclaim-detection regex, and a negated "does not guarantee ease"); all internal PSA links and all 28 ladder lesson hrefs resolve on disk. Zero simplified ZWDS terms remain in content. FABLE acceptance checklist: all boxes pass (real-browser confirmation pending).

**NOT verified this session.** Real-browser render (sandbox npm blocked, no jsdom; extension cannot open `file://`). Open the hub #readers-path, the four child pages, and the chart page from disk to confirm the teach-back model, the Imperial exam, and the converted characters render.

**Deviations / deferred.** The Imperial exam is a fresh 20 covering the whole path rather than the chart page's original 10 plus 10 (self-contained in data, higher coverage; the chart-page Foundation 10 stays as-is). The shared mega-nav and footer ZWDS terms were converted in the shipped files but the durable fix belongs in `components/` source. Model 7 walkthrough UI, the annotated study-chart highlighting, and the full Mei re-cast to Metal Bureau remain optional polish. Karma stays dark until the server `zwds_*` kinds exist.

**Status.** Waves 1-5 complete. The Purple Star Astrology hub is a working learning academy: cast, learn, practice, teach it back, and sit the Imperial exam, all keyboard-operable, colorblind-safe, sound-off and reduced-motion safe, with no deterministic claims and no account-gated learning.

## Wave 4 — 2026-07-07 — SHIPPED

Casting and depth. The chart-casting engine, the Rui sample chart, Model 6 (Timing wheel) with the Three Clocks block, the `supporting-stars/` child page (L6 home), and L4–L7 wiring. Built per PSA-MASTER-PLAN §4/§5 and doc 04/05. **Uncommitted; commit manually.**

**Casting engine (`ziwei-caster.js`, NEW).** The deterministic orthodox ZWDS construction: Life and Body palace from lunar month + birth-hour branch; palace stems by the Five Tigers rule; the Five Element Bureau by the nayin of the Life Palace's stem-branch; Zi Wei placement by the 起紫微 bureau-and-day method; Tian Fu as Zi Wei reflected across the 寅-申 axis; the 14 principal stars by their fixed offsets; the four transformations by year stem; decade doors with direction by year-polarity and gender; Ming Zhu / Shen Zhu. **Validated exactly:** the engine reproduces Mei's 12-palace star layout and transformations with **0 mismatches** (node assert), and three hand-checked Zi Wei cases pass (Water 2 day 1 → 丑, Water 2 day 2/3 → 寅, Metal 4 day 10 → 午). Provenance is logged honestly: doc 04 describes the method but not the numeric tables (Needs-Source S1–S4, S6), so the tables are the standard construction algorithm whose correctness is established by the reproduction test, per D14. Gregorian→lunar conversion (S9) is out of scope this wave, so the engine casts from lunar inputs and the sample charts are defined by theirs.

**FOUND DISCREPANCY (logged, not silently patched).** The chart page narrates Mei as 火六局 Fire Bureau with her first decade door at age 6. A 丙子 (1996) year with the Life Palace in 午 derives to 金四局 Metal Bureau (first door age 4) under the orthodox nayin rule. Mei's star layout and transformations are algorithm-consistent and reproduced exactly; her stated bureau and first-door age are a chart-page teaching liberty. The engine returns both the stated and the derived bureau (`bureauMatchesDerived: false`). Recommend correcting the chart-page bureau in the Wave 5 sweep.

**Rui, born 1988 (`ziwei-sample-charts.js`).** A second worked chart, constructed with the engine and validated by it (0 mismatches): 戊辰 year, Earth Bureau, first door age 5 advancing forward. Contrasts Mei on every teachable axis that the real algorithm allows: 命宮 holds 天府 the steward (not 紫微 the emperor); two empty palaces (Siblings, Children) that must borrow their opposite's stars; the natal Hua Ji falls on 天機 in the Health palace (a constitution-and-mind recurring lesson, where Mei's Hook is a career lesson). DELTA FROM PSA-HUB-EXPERIENCE §6.7 (logged, arithmetic wins per the spec's own instruction): the sketch asked for "Hua Ji on 貪狼 in Spouse" and "an empty Wealth palace," neither of which is possible for a real 戊辰 chart with 天府 in 命宮 (戊's Hua Ji is 天機, and Wealth here holds 紫微). The engine's true output is used and the steward-not-emperor contrast preserved.

**Model 6 Timing wheel + Three Clocks (hub #timing).** A collapsed-by-default disclosure ("assumes you know the palaces and the triangle; the control always works", never locked): the twelve decade doors with their age ranges, an age input that opens the matching door (門 glyph + ages, shape+text), and the Year Wave marking the palace whose branch matches the current year (☀ + "this year"), all running on Mei's sample chart. The verdict keeps natal promise and temporal trigger distinct: "The door sets the decade's theme. The year knocks on one room. Neither rewrites the birth chart." Opening a door completes lesson 7.2 and marks the timing hall. Beneath it, the **Three Clocks** block disambiguates the three timing systems a learner meets on the site (the almanac's day quality, BaZi luck pillars, ZWDS decade/annual cycles) with the exact bridge copy for B4 (`/bazi/luck-pillars/`) and F2 (`/almanac/`), stated as three separate calculations that are never mixed. Both external bridge targets exist on disk.

**Supporting Stars (`supporting-stars/`, NEW) + `ziwei-auxiliary-stars.js` (NEW).** A self-contained, file://-safe L6 page rendering the conditioning layer from doc 05 Topic 1: the two Assistants, the two Nobles, the two Scholars, the Four Sha, the Wealth Retainer 祿存 with its "prosperity flanked by blades" rule, the two Voids, and the movement and romance stars, plus a 格局 patterns note. Every star carries a productive face and a corrosive face (doc 05's "auspicious is not always good, malefic is not always bad"), ruby pinyin, and a pronounce button (the data file registers speak strings into the pronunciation map). The one rule leads the page: never read a supporting star alone. The subnav's "Auxiliary Stars" item is now a live link and "Timing" points to the hub #timing model.

**L4–L7 wiring.** `ziwei-lessons.js` un-gates Levels 6 and 7 (their pages and the #timing model now exist), so every L1–L7 lesson is reachable from the #your-path ladder without an account; L8 remains coming until Wave 5.

**Verified this session.** `ziwei-caster.js`, `ziwei-auxiliary-stars.js`, and the edited `ziwei-sample-charts.js` / `ziwei-lessons.js` `node --check` clean; all twelve `ziwei-*.js` load together with a stubbed window. Caster reproduces Mei (0) and Rui (0) exactly; the three hand-checked Zi Wei cases pass; the Mei Fire-vs-Metal discrepancy is confirmed programmatically. All inline scripts across the hub (6 blocks), the supporting-stars page (1), and the chart page (3) parse; script tags balanced (hub 20/20, supporting-stars 4/4, chart 8/8). Zero duplicate ids; ruby/rt balanced (hub 133/133, supporting-stars 2/2); zero simplified characters, banned strings, arrows, or underlined links on the new surfaces; the wave-4 mount points are present and the B4/F2 bridge targets exist.

**NOT verified this session.** Real-browser render (sandbox npm blocked, extension cannot open `file://`) — open the hub (#timing), the supporting-stars page, and confirm the timing wheel opens doors, the Year Wave marks the Command palace, and the Three Clocks copy reads distinctly.

**Deviations / deferred.** Model 7 (the sample-chart walkthrough with a Rui tab) is not a Wave 4 item in master-plan §5 and is deferred (Rui's chart data is shipped and ready for it). The annotated study-chart highlighting on the chart page (learned concepts lit) is deferred to the Wave 5 sweep; the chart page already shows Mei fully, with nothing blurred or locked, so the "no paid locks" rule holds today. Casting from a Gregorian birthday awaits the S9 lunar-conversion source.

**Next action.** Wave 5 — the Reader's Path and the sweep: L8 (synthesis, ethics, teach-backs, consultation structure labeled house method), the 20-question Imperial exam, the remaining connection-map bridges, the Karma bridge only if the server `zwds_*` kinds exist, and the full simplified-character + trust sweep of all child pages (including correcting Mei's chart-page bureau), plus the production audit and the FABLE-PROMPT final checklist.

## Wave 3 — 2026-07-07 — SHIPPED

The academy. The Reader's Path curriculum UI, the `zwdsSchool.v2` progress store with v1 import and mirror-back, the six-rank ladder with legacy grandfathering, the Foundation Exam rebrand, the glossary child page, Model 4, and the CTA ladder. Built per PSA-CURRICULUM.md (Parts 1–5) and PSA-MASTER-PLAN §5. **Uncommitted; commit manually.**

**New data files.**
- `ziwei-progress.js` (NEW): the `zwdsSchool.v2` store. Imports the flat `zwdsSchool.v1` once (never deletes or restructures it), mirrors the five legacy hall flags (wheel/case/drills/timing/exam) back to v1 on change so the chart page's progress strip keeps working, tracks comprehension events only (lesson_completed, level_completed, check_passed, exam_passed, rank_earned, chart_cast) and dispatches a `psa:progress` document event for each. Six-rank ladder (Court Novice → Palace Scribe → Star Keeper → Warden of the Forces → Keeper of the Doors → Imperial Astrologer) earned by level mastery; the old 10-question exam confers at least Star Keeper; **legacy exam ranks are grandfathered forever as a rank floor** (`legacyRankGrandfathered`). `hasProgress()` gates the account CTA. **Karma bridge intentionally NOT wired (D10):** the `zwds_*` earn kinds do not yet exist in the server allowlist, so no `ZodiKarma.award` call and zero Karma copy anywhere; `psa:progress` events are emitted so the bridge can be added later without touching lesson code.
- `ziwei-lessons.js` (NEW): all 8 levels and 67 lessons (id, title, source page/anchor, level rank, one-sentence completion ability; L1–L3 carry `hub` action hooks). Counts verified 6/13/16/6/5/8/6/7 = 67.

**Hub (`elements/purple-star-astrology/index.html`).**
- **#your-path** section: the progress-aware eight-level ladder with per-level state (done / you-are-here / ahead / arrives-soon), lesson-progress counts, and a link to each wired level (L1–L5 point to existing pages; L6–L8 shown without links until their pages ship). **"Save this path" appears only after `hasProgress()` is true** (first comprehension event), and its sub-line says plainly "Free, and it saves, it never unlocks."
- **Model 4 Build-a-chart-sentence** (#chart-sentence): three ingredient pickers (Star, Room, optional Transformation) as button-listboxes, a textarea, a Compare button gated on star+room+≥20 chars (with visible gate text, not a grey button), then model answers (A beginner would say / A practitioner would add, seeded from `ziwei-practice.js` with a placement fallback, plus the transformation's natal effect line), an "Evidence you did not have" list, and a three-button self-check. Overclaiming is the taught failure: choosing "I overclaimed" (or using flagged words will/never/always/doomed/guaranteed/destined/must/cannot) quotes the words back and offers the uncertainty rewrite. The self-check writes a comprehension event.
- **Live progress wiring:** the header stage chip, the subnav progress chip, and the left-rail stage card + 8-segment track now read from `ZiweiProgress` and repaint on every `psa:progress` event.
- **Comprehension hooks:** Model 1 selecting three distinct rooms completes lesson 1.3 (and marks the wheel hall); Model 2 seating the Emperor completes 3.2; Model 3 applying two distinct stems completes 4.0; Model 5 lighting all four rooms completes 5.2 (and marks the case hall). Wave 2 exposed `window.PSA` (sound + reinforcement) so Model 4 can trigger them.
- **CTA ladder:** the #chart-sentence transition upgrades to the "cast it as your study map" copy after three lessons; the #your-path account CTA appears only after visible progress. Subnav Reference → Glossary is now a live link.

**Glossary child page (`glossary/`, NEW).** Self-contained, file://-safe (inline hub tokens, relative `../../../js/ziwei/` data loads), breadcrumb + back-to-hub. Renders every core term from `ziwei-glossary.js` with ruby pinyin, a pronounce button, literal, standard English, the editorial title labeled "our title, not a translation," plain meaning, reader's note, and source notes. DefinedTermSet JSON-LD, canonical on the www domain.

**Chart page Foundation Exam rebrand.** "The Reader's Exam" → **"The Foundation Exam"**; the 10-question exam now tops out at **Star Keeper 司星** (9+), with Imperial Astrologer reserved for the full Reader's Path 20-question sitting. The page loads `ziwei-progress.js` (relative path, so it also renders from disk) and records the result through `recordExam("foundation", …)`, which mirrors the exam hall to v1 and floors the displayed rank at any grandfathered legacy rank ("honored from the first graduating class"). Share copy updated; em-dash separator removed.

**Verified this session.** `ziwei-progress.js` and `ziwei-lessons.js` `node --check` clean; all ten `ziwei-*.js` load together with a stubbed window (30 namespaces, 67 lessons, `ZiweiProgress` present). Store behavior tested with a seeded v1 blob: v1 imported (`migratedFromV1`), legacy Imperial rank grandfathered and honored as the floor after completing L1–L3, `markHall("timing")` mirrored back into v1, a fresh store reports no progress and null rank, and `hasProgress()` flips true after one lesson. All inline scripts across the hub (5 blocks), glossary (1), and chart page (3) parse; JSON-LD valid; script tags balanced. Zero duplicate ids; ruby/rt balanced (hub 127/127, glossary 4/4); zero simplified characters, banned strings, arrows, or underlined links on the new surfaces; all L1–L5 ladder link targets exist on disk.

**NOT verified this session.** Real-browser render (sandbox npm blocked, extension cannot open `file://`) — open the hub, the glossary page, and the chart page from disk to confirm Model 4 compares, the path ladder paints with progress, the chips update, and the exam awards Star Keeper. Static, data-integrity, and store-logic checks are complete.

**Deviations logged.** Full per-lesson delivery UIs for all 67 lessons are not built; the ladder links to the existing pages where each lesson's content lives, and four hub actions fire real comprehension events. Level-completion (whole-level mastery checks) is wave 4+; until then the ladder shows partial per-level lesson counts and levels do not flip to "Complete" through normal hub use. Model 4's self-check records `passCheck("model4", …)` as a lightweight comprehension marker (not a formal level id). The glossary page is self-contained rather than using the baked mega-nav so it renders from disk; nav/footer build markers can be added when the build runs.

**Next action.** Wave 4 — casting and depth: the casting engine (`ziwei-caster.js`, must reproduce Mei exactly), the annotated study chart, the Rui 1988 sample chart, Model 6 (Timing wheel) + the Three Clocks block, L4–L7 wiring, and the new `supporting-stars/` child page (L6 home), then un-disable its subnav item.

## Wave 2 — 2026-07-07 — SHIPPED

The living hub. Models 2, 3, 5; both rails + IntersectionObserver; the five rolling widgets; the Web Audio sound system; the reinforcement copy set; the mobile bottom sheet. Built per PSA-MASTER-PLAN §5 and PSA-HUB-EXPERIENCE §2–§9. **Not a git repo from this mount: changes are uncommitted, commit manually.**

**Data layer.**
- `ziwei-principal-stars.js`: all 13 previously-null stars now carry `placements` (12 palaces × 4 layers: beginner / intermediate / practitioner / misread). zi-wei upgraded from beginner-only strings to the same 4-layer object; its beginner layer keeps the shipped teaser wording, the three deeper layers authored from docs/zwds/02 §1 + PSA-TERMINOLOGY §1.3. Extraction was parallelized across three subagents reading each star page's "Across the 12 Palaces" grid (`.palace-desc`), cross-checked against doc 02; every entry rewritten to non-deterministic voice per PSA-TERMINOLOGY §3.2/§7 (qi-sha Spouse determinism, tian-liang shelter, tan-lang Peach Blossom all softened to "classical readings associate / tends to"). Each star carries `placementsSource`. 168 placements verified programmatically (all 4 layers present, zero empty), sensitive entries spot-checked against source.
- `ziwei-transformations.js`: each of the four forces gains `linePattern` (Lù solid / Quán double / Kē dashed / Jì dotted, colorblind-safe), `thread` (the 5.3 one-liner), and `natalFrame`/`timingFrame`/`natalEffect`/`timingEffect` templates with a `{star}` slot for Model 3.
- `ziwei-sample-charts.js` (NEW): Mei (born 1996) copied verbatim from the chart page PALACES array (12 rooms, seated stars, borrowed rooms, Fire Bureau, first door age 6, 1996 丙 natal Hua), plus the Read-the-Triangle interpretation fragments (Career scenario, additive base → +mirror → +tri1 → +tri2 → full). No real birth data (§7.8).
- `ziwei-practice.js` (NEW): 8 comprehension drills tagged reading-order / transformations / synthesis for the right-rail practice card, chart-page `renderQuiz` shape; plus a small Model-4 sentence seed for wave 3.

**Hub page (`elements/purple-star-astrology/index.html`).**
- Three new sections in curriculum order after #the-court: **#the-stars** (Model 2 Move-a-star: 14-seal tray grouped by series, focus court, drag OR pick-then-place, four disclosure layers, "one placement is never the reading" persistence copy that strengthens after 4 distinct seats), **#four-transformations** (Model 3: birth-year field + ten stem chips, four force cards with the star each stem flies to, natal + timing effect lines, the thread line-patterns, the standing Hook caution), **#read-the-triangle** (Model 5: Mei's Career room, three toggles + Mirror / + Triangle ×2, verdict grows more qualified with each room, reaches the five-step miniature at 4/4).
- Model 1 now publishes to the shared `psa:select` bus; renderPanel reads the new 4-layer placement shape (`.beginner`).
- **Left rail "Your path"** (stage card + 8-segment discrete track, current/next lesson, chart status) and **right rail "The living court"** (one deck of ≤3 cards chosen by the centered section via IntersectionObserver, `rootMargin: -40% 0 -40%`, aria-live deck announcements, a Pause control persisting to `zodi_psa_motion`). Rails render ≥1200px; below that everything moves into the **mobile bottom sheet** (two real tabs, focus-trapped dialog, Escape/close) opened from a sticky continue-lesson bar.
- **Five rolling widgets:** Court in Motion (14 seals drifting by series, hover/focus pause, click opens a learning card; static two-row grid when paused/reduced-motion), Palace Orbit (SVG 12-node ring, chords dashed=mirror solid=triangle, keyboard-navigable, publishes selection), Transformation Thread (auto-advancing 4-force threads, chips to select, all-drawn when paused), Term of the Moment (glossary rotation, click to freeze+expand), Chart Pulse (read-only 4×4 that only changes on `psa:select`; surfaced in the default deck since its spec homes #timing/#walkthrough are wave 4).
- **Sound system:** Web Audio, synthesized, **off by default** (`zodi_psa_sound`), one toggle in the subnav (and mirrored in the sheet). Correct-note on right answers, two-note resolution on lesson-complete (Model 5 full), per-family placement tone on seating (Throne/Court/Vanguard). **Wrong answers are silent** plus a text correction. Master gain capped low, AudioContext lazily created on first sound-on. Pronunciation stays exempt (pressing a say-btn is consent) and always available.
- **Reinforcement copy:** the 12 in-world lines, fired only on comprehension events (Model 5 full, correct practice answer), max one per two minutes, no repeats per session.
- Subnav: sound toggle added; Study→Transformations now targets #four-transformations and Study→Relationships is live to #read-the-triangle.

**Verified this session.** All four inline scripts `node --check` clean; JSON-LD parses; 15/15 script tags balanced. All eight `ziwei-*.js` parse and load together with a stubbed window; `relationships.court("ming-gong").members` = 命財官遷 (assertion passes); 14/14 stars carry full placements; Mei's triangle geometry (focal 官祿, trines 命/財, mirror 夫妻) correct; the four line-patterns correct. Zero duplicate ids (excluding the known baked-nav `moon-full-t/d`); ruby/rt balanced 125/125; zero simplified characters, zero banned strings ("kept secret", "already written", "guarantee/guaranteed", determinism greps), zero ↗, zero `text-decoration: underline` on the hub or new data files; zero `elements/zi-wei` refs on touched surfaces. All 22 JS mount-point ids present in the DOM; all `data-scroll` targets exist.

**NOT verified this session.** Real-browser render — the sandbox npm registry is blocked (403) so a jsdom headless run was not possible, and the Chrome extension cannot open local `file://`. Open the hub from disk to confirm the models paint, the rails follow scroll, the bottom sheet opens, and sound toggles. Static verification (syntax, data integrity, mount points, geometry, trust/script lints) is complete; DOM-runtime behavior is reviewed by hand but not executed.

**Deferred to wave 3+.** Live progress chip + `zwdsSchool.v2` migration (rails currently render empty-store defaults); Models 4/6/7; glossary child page; the #chart-sentence / #timing / #walkthrough / #your-path / #reference sections (Chart Pulse's spec homes). Karma stays dark (D10). The wave-1 interim #explore-the-court section remains until later waves restructure the page tail.

**Next action.** Wave 3 — the academy: curriculum UI for L1–L3, `zwdsSchool.v2` storage with v1 import + mirror-back, six-rank ladder with legacy grandfathering, Foundation Exam rebrand, full CTA ladder, glossary child page, Model 4 (Build-a-chart-sentence).

## Wave 1 — 2026-07-07 — SHIPPED

**URL move.** `site/elements/zi-wei/` renamed to `site/elements/purple-star-astrology/`. All 1,042 internal references swept (html/js/xml/txt), zero leftovers. Two 301 rules added to the top of `site/vercel.json` (`/elements/zi-wei` and `/elements/zi-wei/:path*`); Vercel preserves query strings by default. Not a git repo from this session's mount: **changes are uncommitted, commit manually.**

**Data files.** New `site/js/ziwei/` (window.ZiweiData, plain browser JS, file:// safe): palaces (12, full ladders + geometry + aliases), principal-stars (14; zi-wei carries all 12 placements from the teaser, other 13 `placements: null` until wave 2), transformations (4 + 10-stem table), relationships (opposite/trine/court, verified: Life court = 命財官遷), pronunciation (46 zh-CN speak strings + speak()), glossary (20 terms). All `node --check` clean; court math assertion-tested.

**New hub page.** `elements/purple-star-astrology/index.html` replaced (1,185 lines): orientation header with 紫微斗數 pronounce button and teaser-handoff line; sticky PSA subnav (Start Here / Chart / Palaces / Stars / Practice + Study and Reference groups, unshipped items as labeled "coming" states; static progress chip "Court Novice · Level 1 of 8"); Model 1 Twelve Palace Court (keyboard: arrows/Home/End/Enter; aria-live; △ triangle and ◇ mirror shape+text markers; full term ladder per palace); `?star=zi-wei&palace=X` deep link with alias resolution and Emperor placement reading; first contextual CTA to stars/; explore-the-court; Among the Systems (B1 BaZi bridge); rail containers stubbed hidden for wave 2. Data scripts loaded with relative paths so file:// works.

**Live-error corrections.** Hub ×4 + teaser "kept secret 1,000 years" claims replaced with qualified wording; teaser "Yours is already written" → "Yours is already cast. What it means is the part you learn to read."; qi-sha Spouse Palace determinism fixed; history Polaris "purple-violet glow" fixed; palaces COURTS arrays 2/3/4 corrected (C2); teaser "Enter the star court" CTA now passes `?star=zi-wei&palace=<selected>`; 化権→化權 in docs 01/03; doc 04 court table annotated; canonicals/og:url on all 20 pages = zodianimal.com + new path.

**New ruling C8 (found during build).** Doc 03's mirror-pair table had two geometrically impossible pairs (Wealth-Health, Fortune-Parents). Corrected to Wealth-Fortune 財福線 and Health-Parents 父疾線 in PSA-TERMINOLOGY (§1 rows + 對宮 row + C8), doc 03 annotated, data files implement the geometric pairs.

**Verified.** Zero "kept secret / already written / purple-violet" on touched surfaces; zero old-path references outside the redirect sources; vercel.json valid; all inline JS on hub and teaser parses (JSON-LD valid); no underlined links, no ↗, no simplified chars on new surfaces; subnav destinations exist. Known pre-existing: duplicate `moon-full-t/d` ids inside the baked global nav sitewide (not introduced here). NOT verified this session: real-browser render (extension cannot open file:// URLs) — open the hub locally to confirm; and deployed 301 behavior — check after next deploy.

**Deferred to wave 2+:** 13 stars' palace placements, rails, Models 2-7, rolling widgets, sound, scrollspy, live progress chip.

### Wave 1.1 addendum (same day, after live review)

- **file:// fix.** The hub's CSS and nav.js links were root-absolute and broke when opened from disk; converted to relative (`../../css/...`). Rest of the site still uses absolute asset paths and only renders styled when served; sitewide relative sweep is optional future work.
- **Proverbs Pond voice.** `ziwei-pronunciation.js` speak() now uses the exact zaSpeak voice ranking from `js/proverbs-share.js` (Tingting/Meijia > Google Mandarin > best zh, rate 0.78) and defers to `window.zaSpeak` when present. One Mandarin voice sitewide.
- **Ruby pinyin.** Per-character `rubyPinyin` arrays added to palaces (12), stars (14 + 4 groupings), transformations, glossary (20); shared `ZiweiData.rubyHtml()` helper. Hub renders pinyin above every Chinese character (hero, all palace cards, detail panel, center seal, subnav items, section labels), matching the Proverbs Pond `<ruby><rt>` pattern. 113 ruby elements verified 1:1 with rt. **Convention forward: every new PSA surface uses rubyPinyin + rubyHtml, never a separate pinyin line next to characters.**
